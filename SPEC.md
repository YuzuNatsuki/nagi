# 凪 (nagi) 仕様要約

## プロダクトの一行定義

「AI に話しかけるアプリは、たくさんある。  
凪は、AI が、あなたに、大切な人について、話しかけるアプリ。」

凪は、利用者本人の気分管理ではなく、観察対象となる大切な人について、AI が察しを通知として届ける体験を中心に置く。  
観察対象と受信者は分離され、チャットは通知体験を補助するための機能として扱う。

## 3 エージェント構成

- observation agent
  - mood / whisper / ペア状態などを入力として観察情報を整理する。
- interpretation agent
  - 観察情報を推察に変換し、通知が必要かを判定する。
- notification agent
  - 通知本文を生成し、通知配送基盤に受け渡す。

上記 3 役割は分離し、単一 LLM に統合しない。  
通知文生成とチャット応答生成は別プロンプト・別予算で管理する。

## 共有ペア (v2) の要点

- 利用は最大 4 人の共有ペアを前提とし、単独モードは持たない。
- 参加は招待コード入力とオーナー明示承認の二段階で成立する。
- ペア内共有は AI が生成した通知本文のみを対象とする。
- mood / whisper の生データは本人のみ参照可能で、他メンバーには非公開。
- Phase 1 でも認可の形を本番準拠にし、他人データへのアクセスは 403 を返す。

## 補助機能: 凪に話す (チャット) の要点

- チャットは補助機能であり、通知体験の代替にしない。
- 起動は常にユーザー主導とし、AI 側からのプッシュ誘導を行わない。
- 「誰について話すか」は `特に指定なし` とペアメンバーから選択できる。
- ペアメンバーを話題にする応答は推察口調を守り、断定を避ける。
- 話題の制限は設けず、仕事・料理・雑談にも短く自然に応じる。
- 履歴保持は `保持しない / 30日 / 90日` を選択でき、既定は 30 日とする。

## トーン規則のサマリ

- 普通の日本語で短く書く。
- 命令形を使わない。
- 絵文字を使わない。
- 数値スコア、順位、比率による評価を出さない。
- 過剰敬語やビジネス文体を避ける。
- 判断は受信者本人に残し、通知が不要なら `should_notify=false` を返す。

この規則は通知文、UI 文言、チャット応答、コミットメッセージまで一貫して適用する。  
運用では「プロンプト制約 + サーバ側フィルタ」の二段ガードで崩れを抑える。

## 視覚デザインの哲学

- 配色は 3 色まで: 紙の白、濃いグレー、差し色 1 色。
- 余白を広く取り、1 画面の情報密度を低く保つ。
- 本文は Noto Sans JP、見出しは Noto Serif JP を控えめに使う。
- 動きは 500ms 程度のフェード中心で、強い演出を避ける。
- 観光的な和モチーフは使わない。

## セキュリティとプライバシー

- デバイストークンは KMS による暗号化保管を基本とする。
- 通知本文は永続化せず、履歴には message hash のみ保存する。
- 招待コードは暗号論的乱数で生成し、24 時間で失効、使い切りとする。
- 全ペアイベントは `pair_audit_log` に追記する。
- 本人データ API とペア共有 API の責務を分離し、誤公開を防ぐ。

## 禁止事項

- 機嫌度、親密度、ランキングなどの評価指標を導入しない。
- 常時録音、常時位置追跡、依存誘導を導入しない。
- AI からチャット起動を促す通知を送らない。
- 観光的な和モチーフを UI に使わない。
- 通知本文を Firestore に保存しない。
- 単独モードを実装しない。
- Unit 3 の Vertex AI、Unit 5 以降の Firestore、KMS / Secret Manager をスタブのまま放置しない。

## Phase 1 の方針 (モックアップ)

- `npm run dev` でフロントエンドと Express ダミー API を同時起動する。
- ダミー API は本実装と同一レスポンス shape を返す。
- 通知文とチャット応答は固定文ではなく、入力傾向に応じた分岐で変化させる。
- API 応答にはランダム遅延を入れ、ローディング体験を検証する。
- 画面は「最初に出会う順」で 1 画面ずつ完成させ、毎回確認して進める。

## Phase 2 の方針 (本実装)

- 実装順は `Unit 5 -> Unit 3 -> Unit 4 -> Unit 1 -> Unit 2 -> 仕上げ`。
- 各ユニットは `TypeScript strict`, `tsc --noEmit`, 単体テスト全通過 (property-based test を最低 1 件含む) を満たす。
- ローカルでは in-memory fallback で動作し、feature flag ON で GCP 実サービスへ切り替える。

## API 境界の集約方針 (決定)

ダミー実装と本実装の切り替えはバックエンドの 1 ファイルに集約する。  
具体的には `backend/src/adapters/runtime-backend-adapter.ts` を唯一の切替点にし、  
`RUNTIME_BACKEND_ADAPTER` という固定識別子で `grep` 一発で特定できるようにする。

- ルートハンドラは常に `runtime-backend-adapter` の公開インターフェースのみ参照する。
- Phase 1 は `inMemoryAdapter` を使用する。
- Phase 2 は feature flag により Firestore / Vertex AI / Cloud Tasks などの実アダプタへ切り替える。
- フロントエンド側は API クライアントの呼び出し契約を固定し、切替責務を持たない。
